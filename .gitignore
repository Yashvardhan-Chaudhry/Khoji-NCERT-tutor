# --- Dynamic working docs: kept local, not published ---
# The live control/decision docs for the author + Claude. Locally renamed with a
# `khoji_` prefix for easier traversal — the glob keeps ALL such working docs out
# of the public repo (guards against an accidental `git add -A`). A polished public
# claude.md is added later, deliberately.
khoji_*.md
EVAL.md
EVAL.jsonl
docs/

# --- Data: NCERT PDFs (copyright) + the vector store (bulky, regenerable) ---
data/

# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env

# --- OS / editor ---
.DS_Store
Thumbs.db
.vscode/
.idea/
